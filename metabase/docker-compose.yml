version: '3.8'

services:
  metabase:
    image: metabase/metabase:latest
    container_name: metabase-billing
    ports:
      - "8080:3000"
    environment:
      - MB_DB_FILE=/metabase-data/metabase.db
    volumes:
      - ./data:/metabase-data
    restart: unless-stopped